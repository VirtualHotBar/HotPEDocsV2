# 开发环境

本文档用于说明本仓库（HotPEDocs）在本地的开发/预览环境搭建方式。

## 前置要求
- Node.js：建议使用 18+（更推荐 20+ LTS）
- 包管理器：使用 `npm` 即可

## 本地启动
在仓库根目录执行：
- 安装依赖：`npm install`
- 启动开发服务器：`npm run dev`

启动后会输出本地预览地址（默认端口 `3731`）。

## 构建与部署预览
- 构建静态站点：`npm run build`
- 本地预览构建产物：`npm run serve`
